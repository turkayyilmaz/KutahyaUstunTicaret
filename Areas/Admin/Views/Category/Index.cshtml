@model List<Category>
@{
	ViewData["Title"] = "Index";
	Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
	int count = 0;
}
<div class="container mt-5">
	<h2>Kategoriler</h2>
	<hr />
	<a asp-area="Admin" asp-controller="Category" asp-action="Create" class="btn btn-dark mb-3">Yeni Kategori Ekle</a>
	<table class="table">
		<thead>
			<tr>
				<th scope="col">#</th>
				<th scope="col">Kategori</th>
				<th scope="col">Güncelle</th>
				<th scope="col">Sil</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				count++;
				<tr>
					<th scope="row">@count</th>
					<td>@item.CategoryName</td>
					<td><a class="btn btn-dark" asp-area="Admin" asp-controller="Category" asp-action="Edit" asp-route-id="@item.CategoryId">Güncelle</a></td>
					<td><a class="btn btn-danger" asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-id="@item.CategoryId">Sil</a></td>
				</tr>
			}
		</tbody>
	</table>
</div>